{
  "name": "@autoops/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@autoops/core": "workspace:*",
    "@autoops/db": "workspace:*",
    "@octokit/app": "^15.1.1",
    "zod": "^3.24.2"
  }
}
